"""Entry point for the `pygalago` CLI command."""

import sys


def main() -> None:
    print("PyGalago — Galago search engine (Python + C++ port)", flush=True)
    print("Available sub-commands: (none yet — see REFACTORING_PLAN.md)", flush=True)
    sys.exit(0)
